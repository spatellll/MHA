# Mental Health Access: Community-Level Analysis Example

1. Clone this repository
2. Create folders `data/`, `result/`, `model/`, `analysis/`
3. Move input data files into `data/` (provided)
4. Run `demo.ipynb` notebook (check the list of required packages in the notebook `import...`)
